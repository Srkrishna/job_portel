<%= form_for(requested_job) do |f| %>
  <% if requested_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(requested_job.errors.count, "error") %> prohibited this requested_job from being saved:</h2>

      <ul>
      <% requested_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :emp_id %>
    <%= f.number_field :emp_id %>
  </div>

  <div class="field">
    <%= f.label :job_id %>
    <%= f.number_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :company_id %>
    <%= f.number_field :company_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
