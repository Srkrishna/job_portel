<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company_detail.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @company_detail.address %>
</p>

<p>
  <strong>Keyword:</strong>
  <%= @company_detail.keyword %>
</p>

<p>
  <strong>Description:</strong>
  <%= @company_detail.description %>
</p>

<p>
  <strong>Url:</strong>
  <%= @company_detail.url %>
</p>

<%= link_to 'Edit', edit_company_detail_path(@company_detail) %> |
<%= link_to 'Back', '/company_details' %>

<%@job_posts = JobPost.where(:company_id=>@company_detail.id)%>
<%p @job_posts%>
<%p "=========================================="%>
<h1>Job Posts</h1>
<%= link_to 'New Job Post', new_job_post_path %>
<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Required on</th>
      <th>Etc</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @job_posts.each do |job_post| %>
      <tr>
        <% tmp = CompanyDetail.find(job_post.company_id) %>
        <td><%= tmp.name %></td>
        <td><%= job_post.required_on %></td>
        <td><%= job_post.etc %></td>
        <td><%= job_post.description %></td>
        <td><%= link_to 'Show', job_post %></td>
        <td><%= link_to 'Edit', edit_job_post_path(job_post) %></td>
        <td><%= link_to 'Destroy', job_post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%end%>
      </tr>
  </tbody>
</table>
